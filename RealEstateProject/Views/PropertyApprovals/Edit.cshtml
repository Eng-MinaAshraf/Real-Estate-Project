@model RealEstateProject.Models.PropertyApproval

@{
    ViewData["Title"] = "Edit Approval";
}

<div class="aqx-page-header">
    <div>
        <h1>Edit Approval</h1>
        <p class="aqx-subtitle">Approval #@Model.ApprovalId</p>
    </div>
</div>

<div class="aqx-detail-card">
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>
        <input type="hidden" asp-for="ApprovalId" />

        <div class="form-group mb-4">
            <label asp-for="AdminId" class="control-label block"></label>
            <select asp-for="AdminId" class="form-control w-full" asp-items="ViewBag.AdminId"></select>
            <span asp-validation-for="AdminId" class="text-danger"></span>
        </div>
        <div class="form-group mb-4">
            <label asp-for="PropId" class="control-label block"></label>
            <select asp-for="PropId" class="form-control w-full" asp-items="ViewBag.PropId"></select>
            <span asp-validation-for="PropId" class="text-danger"></span>
        </div>
        <div class="form-group mb-4">
            <label asp-for="ApprovalDate" class="control-label block"></label>
            <input asp-for="ApprovalDate" class="form-control w-full" />
            <span asp-validation-for="ApprovalDate" class="text-danger"></span>
        </div>
        <div class="form-group mb-4">
            <label asp-for="ApprovalStatus" class="control-label block"></label>
            <input asp-for="ApprovalStatus" class="form-control w-full" />
            <span asp-validation-for="ApprovalStatus" class="text-danger"></span>
        </div>

        <div class="aqx-actions-bar">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </form>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
